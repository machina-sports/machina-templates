agent:
  
  # agent-reporter-players
  name: "agent-reporter-players"
  title: "Agent Reporter Players"
  description: "Agent responsible for generating reporter players"
  context:
    config-frequency: 1
  workflows:
    
    # workflow-reporter-players
    - name: "workflow-reporter-players"
      description: "Reporter Players"
      outputs:
        workflow-reporter-players: "$.get('workflow-status', False)"
