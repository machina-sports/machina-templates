agent:
  name: "agent-onboarding"
  title: "Agent Onboarding"
  description: "Agent responsible for onboarding."
  context:
    config-frequency: 0.04
  workflows:
        
    # soccer-embeddings
    - name: "soccer-embeddings"
      description: "Generate brutally honest Corinthians soccer stats and analysis"
      inputs:
        event_code: "$.get('event_code') or None"
      outputs:
        soccer-embeddings-status: "$.get('workflow-status', False)"
