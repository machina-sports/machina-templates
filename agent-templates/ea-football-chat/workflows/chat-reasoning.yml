workflow:
  name: chat-reasoning
  title: "EA Football Chat Reasoning"
  description: "Routes user message to tool selection (returns [] for now — wired for LLM tool routing)."
  inputs:
    messages: "$.get('messages', [])"
    league: "$.get('league', 'premier-league')"
  outputs:
    tool_calls: "[]"
    workflow-status: "'executed'"
  tasks: []
