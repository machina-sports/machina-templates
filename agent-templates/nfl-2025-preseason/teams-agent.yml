agent:
  name: "draft-nfl-teams-agent"
  title: "Draft NFL Teams Agent"
  description: "Draft NFL Teams Agent."
  context:
    config-frequency: 0.1
  workflows:

    # draft-nfl-teams-update
    - name: draft-nfl-teams-update
      description: "Draft NFL Teams Update"
      inputs:
        input_status: "'waiting'"
        output_status: "'idle'"
      outputs:
        workflow-status: "$.get('workflow-status')"
