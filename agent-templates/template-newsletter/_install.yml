setup:
  title: "Personalized Newsletter"
  description: "Generate a personalized newsletter based on the user's interests and preferences."
  category:
    - special-templates
  estimatedTime: 1 minute
  features:
    - AI Agent to generate a personalized newsletter.
    - AI Reporter to generate a personalized newsletter.
  integrations:
    - machina-ai
    - machina-ai-fast
    - groq
    - openai
    - google-vertex
  status: available
  value: "agent-templates/template-newsletter"
  version: 1.0.0

datasets:
  
  # populate-folders
  # - type: "workflow"
  #   path: "_folders.yml"

  # agents

  - type: "agent"
    path: "agents/generate.yml"

  - type: "connector"
    path: "data/template-compose.yml"

  # prompts
  - type: "prompts"
    path: "prompts/user-analysis.yml"

  - type: "prompts"
    path: "prompts/news-content.yml"

  # workflows
  - type: "workflow"
    path: "workflows/crm-ingestion.yml"

  - type: "workflow"
    path: "workflows/crm-process.yml"

  - type: "workflow"
    path: "workflows/news-content.yml"

  - type: "workflow"
    path: "workflows/user-analysis.yml"

  - type: "workflow"
    path: "workflows/xai-search.yml"
