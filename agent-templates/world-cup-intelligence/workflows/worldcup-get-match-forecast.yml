workflow:
  name: worldcup-get-match-forecast
  title: World Cup Intelligence - Get Match Forecast
  description: |
    Serve the precomputed model forecast for one event and compute the live
    model-vs-market gap (model 1X2 probability vs current market price). The gap is
    computed at read time because markets move intraday; the probabilities come from
    the cached worldcup:model-forecast. Read-only and informational — gaps are not
    value/bet signals.

  context-variables:
    debugger:
      enabled: true
    google-genai:
      credential: $TEMP_CONTEXT_VARIABLE_VERTEX_AI_CREDENTIAL
      project_id: $TEMP_CONTEXT_VARIABLE_VERTEX_AI_PROJECT_ID
  inputs:
    event_urn: "$.get('event_urn', '')"
    include_reasoning: "$.get('include_reasoning', False)"
    min_gap_bps: "$.get('min_gap_bps', 100)"
  outputs:
    forecast: "$.get('forecast', {})"
    model_vs_market: "$.get('model_vs_market', {})"
    analysis: "$.get('analysis', {})"
    status: "$.get('response_status', 'unavailable')"
    availability: "$.get('response_availability', {})"
    evidence: "$.get('response_evidence', [])"
    provenance: "$.get('response_provenance', [])"
    warnings: "$.get('response_warnings', $.get('forecast_warnings', []))"
    required_evidence: "$.get('response_required_evidence', [])"
    unavailable_reason: "$.get('response_unavailable_reason')"
    workflow-status: "$.get('forecast', {}) and 'executed' or 'skipped'"
  tasks:
    - type: document
      name: load-forecast
      description: Load the cached model forecast for this event.
      config:
        action: search
        search-limit: 1
        search-vector: false
      filters:
        name: "'worldcup:model-forecast'"
        value._id: "$.get('event_urn', '')"
      outputs:
        forecast: "($.get('documents', []) or [{}])[0].get('value', {})"
        forecast_warnings: "[] if ($.get('documents', []) or []) else ['No model forecast for this event -- run worldcup-sync-model-forecasts.']"

    - type: document
      name: load-event-markets
      description: Cached markets linked to this event (for the model-vs-market gap).
      condition: "$.get('forecast', {}) != {}"
      config:
        action: search
        search-limit: 200
        search-vector: false
      filters:
        name: "'worldcup:market-cache'"
        value.event_urn: "$.get('event_urn', '')"
      outputs:
        market_outcomes: "[o for d in ($.get('documents', []) or []) for o in (d.get('value', {}).get('outcomes') or [])]"

    - type: connector
      name: compute-gap
      description: Model 1X2 probability vs current market price (informational).
      condition: "$.get('forecast', {}) != {}"
      connector:
        name: worldcup-market-intelligence
        command: compute_model_vs_market_edge
      inputs:
        event_urn: "$.get('event_urn', '')"
        model_probabilities: "$.get('forecast', {}).get('probabilities', {})"
        market_outcomes: "$.get('market_outcomes', [])"
        home_team: "$.get('forecast', {}).get('home_team', {}).get('name', '')"
        away_team: "$.get('forecast', {}).get('away_team', {}).get('name', '')"
        min_gap_bps: "$.get('min_gap_bps', 100)"
      outputs:
        model_vs_market: "$"

    - type: prompt
      name: worldcup-match-forecast-explain
      description: Explain the model probabilities and any model-vs-market gaps (informational).
      condition: "$.get('forecast', {}) != {} and $.get('include_reasoning', False) is True"
      connector:
        name: google-genai
        command: invoke_prompt
        model: gemini-3.5-flash
        location: global
        provider: vertex_ai
      inputs:
        forecast: "$.get('forecast', {})"
        model_vs_market: "$.get('model_vs_market', {})"
        timeout: 120
      outputs:
        analysis: "$"

    - type: connector
      name: build-response-envelope
      description: Report forecast, market-context, and optional reasoning availability independently.
      connector:
        name: worldcup-market-intelligence
        command: build_response_envelope
      inputs:
        capabilities: "{'forecast': ('available' if $.get('forecast', {}) else 'unavailable'), 'market_context': ('available' if $.get('model_vs_market', {}) else 'unavailable'), 'reasoning': (('available' if $.get('analysis', {}) else 'unavailable') if $.get('include_reasoning', False) else 'not_requested')}"
        required_capabilities: "['forecast']"
        evidence: "[{'kind': 'document-cache', 'source': 'worldcup:model-forecast', 'available': bool($.get('forecast', {}))}, {'kind': 'document-cache', 'source': 'worldcup:market-cache', 'outcome_count': len($.get('market_outcomes', []))}]"
        provenance: "[{'provider': 'machina-dixon-coles', 'role': 'forecast'}, {'provider': 'kalshi-polymarket-cache', 'role': 'market-context'}]"
        warnings: "$.get('forecast_warnings', [])"
      outputs:
        response_status: "$.get('status', 'unavailable')"
        response_availability: "$.get('availability', {})"
        response_evidence: "$.get('evidence', [])"
        response_provenance: "$.get('provenance', [])"
        response_warnings: "$.get('warnings', [])"
        response_required_evidence: "$.get('required_evidence', [])"
        response_unavailable_reason: "$.get('unavailable_reason')"
